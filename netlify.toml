[build]
  command = "npm run build"
  publish = ".next"

# Netlify automatically detects Next.js and uses its Next.js Runtime.
# This configuration file ensures the build command and publish directory are explicitly set.
